/**
 * Enum for order item statuses
 */
export enum OrderItemStatus {
  DRAFT = 'DRAFT',
  PAID = 'PAID',
  ACCEPTED = 'ACCEPTED',
  ASSIGNED = 'ASSIGNED',
  CONFIRMED_BY_COURIER = 'CONFIRMED_BY_COURIER',
  PICKED_UP = 'PICKED_UP',
  DELIVERED = 'DELIVERED',
  CANCELLED = 'CANCELLED'
} 